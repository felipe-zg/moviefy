import styled from 'styled-components/native';

export const MovieRow = styled.View`
    flex-direction: row;
    padding: 10px;
    border-radius: 10px;
    margin: 5px 10px;
    background: #ddd;
`;
